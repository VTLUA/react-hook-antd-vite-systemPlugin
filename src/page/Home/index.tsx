import React from "react";
import LayoutCom from "@/component/layout";
const Home: React.FC = () => {
    return (
        <div>
            <LayoutCom />
        </div>
    );
};

export default Home;